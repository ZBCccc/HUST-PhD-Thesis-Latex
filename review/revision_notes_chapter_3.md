# 第三章修订说明

**修订日期**：2026-03-14
**修订文件**：`body/chapter/commitment.tex`
**审稿依据**：`review/review_chapter_3.md`（17个问题：3 Critical, 6 Major, 8 Minor）

---

## 修订总览

共修复 17 个问题中的 17 个（3 Critical + 6 Major + 8 Minor），其中 C2 和 M5 为部分修复（见残留问题）。

---

## Critical 修复

### C1: 命题3.1证明中 p_2=0 的缺口（True Negative 分支）
**修复方式**：双管齐下。
1. 收紧定义3.1的胜利条件：在式(3.2)中增加 `Auth_{i,j}≠∅` 的限定，并在定义末尾显式论证为何 True Negative 分支被排除（其正确性由第二章 QTree 位值健全性独立保障）。
2. 在证明 G_2 分析段落中，显式区分 `Auth_{i,j}≠∅` 和 `Auth_{i,j}=∅` 两种情况，论证 p_2=0 的结论在收紧后的胜利条件下成立。

### C2: 跨章节符号体系不一致
**修复方式**：在章首引言段落后新增"与第二章的符号对应关系"段落，给出 (i,j) ↔ (j,id) 的显式映射表，说明 A_{i,j}^{(Q)} ↔ A_{j,id}^{(t,ν)} 的对应关系。
**残留**：完全统一两章符号需要同时修改 bf.tex，本次仅在第三章内添加映射说明。

### C3: 定义3.1安全实验形式化不充分
**修复方式**：重写定义3.1，结构化为"初始化—对手模型—查询阶段—挑战—胜利条件"五个部分，显式指定：
- 对手角色（恶意服务器）
- 挑战者诚实执行的算法范围
- 对手控制的状态（服务器侧全部状态）
- A_{i,j}^{(Q*)} 的精确计算规则（采样位置由挑战者确定性派生）

---

## Major 修复

### M1: 命题3.2事件分解穷尽性未论证
**修复方式**：在命题3.2证明中新增"穷尽性论证"段落，显式说明：
- 版本回滚攻击由第二章版本锚点签名机制保障
- 候选枚举截断由第二章主项枚举完整性保障
- E_addr ∪ E_bit 覆盖所有导致 Res'≠Res_true 的攻击路径

### M2: Adv^CV 缺少形式化安全实验
**修复方式**：在命题3.2前新增定义3.2（组合可验证性实验 Exp^CV），明确实验流程、对手能力、Res_true 的计算方式、胜利条件。

### M3: 命题3.1证明中 B_2 归约细节不完整
**修复方式**：补充 B_2 的完整构造描述：
- B_2 接收 CR 挑战中的 H_m，本地诚实采样所有密钥（包括 sk_G）
- B_2 维护每棵 Merkle 树的完整内部状态
- 当 E_2 发生时，利用本地状态与伪造路径逐层比对提取碰撞
- 明确说明 B_2 的模拟是完美的

### M4: 签名绑定 I(w) 桶碰撞风险
**修复方式**：在根认证段落后新增桶碰撞安全分析，论证：
- I(·) 非单射，桶碰撞可能发生
- 但 Merkle 树叶子摘要绑定了 xtag^{(r)}，而 xtag 依赖于 H(w)
- 即使 I(w)=I(w')，两个关键词的 Merkle 树叶子内容不同
- 客户端 VfMPath 使用实际恢复的 xtag，路径验证将因叶子摘要不匹配而失败

### M5: 隐私性边界(3.12)缺乏形式化论证
**修复方式**：将式(3.12)显式降级为非正式隐私性声明，明确标注"非定理级结论"。补充三条关键论据供后续形式化参考。
**残留**：完整的形式化模拟论证未给出，需后续补充。

### M6: MPos 键碰撞未在证明中处理
**修复方式**：在命题3.1证明中新增 Game G_{0.5}，显式定义 MPos 键碰撞事件 E_col，利用生日界证明 Pr[E_col] ≤ q_u²·ℓ²·2^{-Ω(λ)}。相应地，命题3.1的优势界新增该碰撞项。

---

## Minor 修复

### m1: l 与 ℓ 混用
**修复**：将 $O(l)$ 替换为 $O(\ell)$。

### m2: 修辞性/推销性措辞
**修复**：替换全部 12 处违规措辞，包括：
- "极其严密的检查逻辑" → "三步检查"
- "坚不可摧的地址绑定链" → "完整的地址绑定验证链"
- "完美契合" → "满足"
- "极高可行性" → "可行性"
- "断崖式降维" → "降低"
- "坚不可摧的组合安全性担保" → "在标准假设下可证明的组合安全性"
- "彻底闭合"（多处）→ "闭合"
- "彻底阻断" → "阻断"
- "绝对不可伪造/绝对不可错配" → "不可伪造/不可错配"
- "丝毫不差/确凿无疑" → 删除
- "完美维持" → "维持"
- "绝对不变" → "不变"
- "完美隐藏" → "隐藏"

### m3: 元写作措辞
**修复**：删除或改写 5 处：
- "下面给出一个具体的" → "以下通过一个具体的...说明"
- "下文将首先形式化定义...进而提出..." → 删除
- "以下给出满足定义3.1的" → "满足定义3.1的...如下"
- "值得强调的是" → 直接陈述
- "值得特别指出的是" → 直接陈述

### m4: 软件工程术语
**修复**：替换 3 处：
- "非侵入性（Non-intrusive）" → "不修改基础协议的语法与安全性"
- "卸载至服务器侧" → "由服务器...按需执行"
- "非侵入式的选择性开封" → "选择性开封...不修改基础协议的语法"

### m5: 算法行号引用
**修复**：核对并修正行号引用：
- "第 14--15 行" → "第 14--16 行"（包含 MTree 存储）
- "第 16--19 行" → "第 17--20 行"（FOR 循环）
- 删除了行内具体行号引用（"第 17 行"、"第 18 行"）

### m6: \cite{nomos} 引用键
**修复**：`\cite{nomos}` → `\cite{bag2024tokenised}`（与 thesis.bib 中的键一致）。

### m7: Proof_{i,j} 未在符号表中定义
**修复**：在表3.1中新增 Proof_{i,j} 和 ProofAddr_{i,j} 的定义条目。

### m8: (Q) 上标语义模糊
**修复**：在首次使用 A_{i,j}^{(Q)} 后补充说明：上标 (Q) 隐含了对令牌随机性 {β_t} 的依赖，采样位置由挑战者确定性派生，对手无法影响。

---

## 残留问题

1. **C2 部分残留**：跨章节符号完全统一需要同时修改 bf.tex。当前仅在第三章添加了映射说明段落。建议后续在 bf.tex 中也添加对应的前向引用。

2. **M5 部分残留**：式(3.12)的隐私性声明已降级为非正式讨论，但完整的形式化模拟论证（构造模拟器证明服务器视图可被模拟）尚未给出。如需提升至定理级别，需补充完整的模拟器构造与不可区分性论证。

3. **m5 行号核对**：算法行号引用已基于当前伪代码结构修正，但若后续修改算法伪代码，需重新核对。

---

## 需要作者确认的事项

1. **C1 修复路径选择**：当前采用"收紧胜利条件"的路径（排除 Auth_{i,j}=∅ 的关系）。如果作者希望地址绑定健全性也覆盖 True Negative 分支，则需要修改算法4的验证逻辑（在 True Negative 分支下增加额外的地址归属检查机制），这将涉及更大范围的协议修改。

2. **M4 桶碰撞分析**：当前论证依赖于"xtag 绑定了 H(w)"这一事实。如果作者认为需要更强的保障（例如将签名消息从 I(w)||rt 改为 H(w)||rt），请确认是否需要修改协议构造。

3. **M6 碰撞界**：命题3.1的优势界新增了 q_u²·ℓ²·2^{-Ω(λ)} 项。该界在实际参数下可忽略，但改变了命题的精确表述。请确认是否接受。
